#include <iostream>
#include <fstream>
#include <string>
using namespace std;

int main() 
{
    int Symbol [] = { };    //initialize array Symbol
    int freq = f;   //frequency of ASCII symbol (need 4 byte int)
    char symbol;    // ASCII symbol
    string String;

    string 
    fstream myFile;     //declare variable of f stream, variable is myFile
     
    //iteratre through the string and create an array that stores each char of string
    
    infile.open ();ifstream infile;     //ifstream to OPEN file  
    while(!infile.eof)  //reads ALL THE WAY through file
    {
        getline(infile, String);    //saves the line into string 
        cout << String; //prints out our string
    }
    infile.close()  //close the file
     //create a new linked list

     //iterate through each char array and check if the character appears in the linked list 

     //iterate through each char array
    int array_size = sizeof(String)
    sizeof(symbol);
    string String = "";
    int i;
    for(i = 0; i < )
    cout << index << "\t" << freq << "\n" << endl;  // will print out index \tab freq \newLine
    return 0;   //end of program
}